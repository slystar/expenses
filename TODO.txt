Models:
- import from bank
    - New Tables
	- rails generate model ImportHistory user_id:integer import_config_id:integer original_file_name:string new_file_name:string
	    - keep filename of uploaded file
	    - create unique filename to store file localy
		- year_md5sum.extension
	- rails generate model ImportConfig user_id:integer title:string description:text field_mapping:text file_type:string unique_id_field:string unique_id_hash_fields:text
	    - field_mapping: JSON or YAML?
		- YAML is easy but SLOW to Create
		- JSON is fater but can only do primitives, hash, arrays
		- www.skorks.com/2010/04/serializing-and-deserializing-objects-with-ruby/
	    - look at builtin rails option (http://stackoverflow.com/questions/6694432/using-rails-serialize-to-save-hash-to-database)
		- serialize :COLUMN, Hash
		- serialize :column, JSON
	- rails generate scaffold ImportData user_id:integer import_history_id:integer import_config_id:integer unique_id:string unique_hash:string mapped_fields:text process_flag:boolean process_date:datetime expense_id:integer process_notes:text
	    - will serve the view, controller portion to do the actual import
	    - Keep original data in file and keep reference to file in database
    - Modification to tables
	- rails generate migration AddImportInfoToExpense imported:boolean import_history_id:integer import_data_id:integer
- duplication check
    - fields: amount, date_bought, store
- check indexes
- expense history (user_id, expense_id, group_id), could allow group
  deletion

Backups:
- put backing up code in model?
